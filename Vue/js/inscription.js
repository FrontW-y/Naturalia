// Obtenez une référence vers le formulaire
const form = document.querySelector('form');

// Créez une fonction pour afficher le formulaire
function showForm() {
  form.style.transform = 'translateY(0)';
}

// Utilisez setTimeout pour déclencher l'animation après un délai de 1000 millisecondes (1 seconde)
setTimeout(showForm, 1000);

document.addEventListener('DOMContentLoaded', () => {
    // Placez le code pour afficher le formulaire ici
  });
  
